import { SafeAreaView, Text } from 'react-native'

export default function Home(): React.ReactElement {
  return (
    <SafeAreaView className="flex-1 bg-black">
      <Text className="text-xl text-gray-700">Home</Text>
    </SafeAreaView>
  )
}
